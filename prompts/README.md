# Prompts Library Storage Directory

This folder is designed to store static versioned prompt files for auditing and reference.
The prompt editor on the dashboard reads/writes directly to the backend database, but we recommend committing final versions of critical system instructions here in raw `.txt` or `.md` files for production deployments.
